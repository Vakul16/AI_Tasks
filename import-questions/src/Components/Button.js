import React from "react";

const Button = () => {
  return (
    <div className="btn">
      <i class="fas fa-plus-circle"></i>
      <button>Add selected questions</button>
    </div>
  );
};

export default Button;
