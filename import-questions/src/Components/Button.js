import React from "react";

const Button = () => {
  return (
    <div className="btn">
      <button>
        <i class="fas fa-plus-circle"></i>
        Add selected questions
      </button>
    </div>
  );
};

export default Button;
